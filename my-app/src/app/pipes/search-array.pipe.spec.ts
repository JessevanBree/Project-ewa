import { SearchArrayNamePipe } from './search-array.pipe';

describe('searchArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArrayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
