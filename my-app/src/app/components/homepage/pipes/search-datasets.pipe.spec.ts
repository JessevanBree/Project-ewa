import { SearchDatasetsPipe } from './search-datasets.pipe';

describe('SearchDatasetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDatasetsPipe();
    expect(pipe).toBeTruthy();
  });
});
