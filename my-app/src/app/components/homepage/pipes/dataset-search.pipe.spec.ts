import { DatasetSearchPipe } from './dataset-search.pipe';

describe('DatasetSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DatasetSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
